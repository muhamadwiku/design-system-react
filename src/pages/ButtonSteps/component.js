import React from "react";

export default function Component() {
  return(
    <section>
      <h1>Lets make a button</h1>
      <div>
        first we need to scratch, what the button can do
      </div>
      <div>
        <ul style={{ textAlign: 'start' }}>
          <li> click an action </li>
          <li> Link to other page </li>
          <li> change the color = make it dynamic in one code of color </li>
          <li> change the shape, radius or something</li>
          <li> change text color </li>
        </ul>
      </div>
    </section>
  );
};
